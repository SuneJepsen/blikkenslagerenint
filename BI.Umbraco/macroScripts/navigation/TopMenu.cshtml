@inherits umbraco.MacroEngines.DynamicNodeContext
@using BI.Repository;
@using BI.Repository.Entities;


@{
    List<NavigationItem> items = NodeRepository.GetTopMenu();
    List<NavigationItem> submenu = new List<NavigationItem>();
}
<!-- Start: Navigation wrapper -->
<section class="large blue" id="top-menu">
    <div class="container navbar " id="navbar">
        <div class="navbar-inner ">
            <a href="/" class="brand brand-bootbus pull-left">@Dictionary.ForsideFirmaNavn <br/><span>@Dictionary.ForsideFirmaMotto</span></a> 
          

            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="nb_left pull-left">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </span>
                <span class="nb_right pull-right">menu</span> 
            </a>
            <!-- Start: Primary navigation -->
            <div class="nav-collapse collapse">
                <ul class="nav pull-right">
                    @for (int i = 0; i < items.Count; i++)
                    {
                        NavigationItem item = items[i];

                        <li>
                            <a href="@item.Link">@item.NavText</a>

                        </li>
                        @*
                        NavigationItem item = items[i];
                        if (item.HasSubmenu)
                        {
                        <li class="dropdown">
                            <a href="@item.Url" class="dropdown-toggle" data-toggle="dropdown">@item.NavText<b class="caret"></b></a>
                            @{
                            submenu = NodeRepository.GetSubMenu(item.Id);
                            }
                            <ul class="dropdown-menu">
                                @for (int j = 0; j < submenu.Count; j++)
                                {
                                    <li><a href="@submenu[j].Url">@submenu[j].NavText</a></li>
                                }
                            </ul>
                        </li>

                        }
                        else
                        {
                        <li>
                            <a href="@item.Url">@item.NavText</a>

                        </li>
                        }*@

                    }
                </ul>
            </div>
        </div>
    </div>

</section>
<!-- End: Navigation wrapper -->

